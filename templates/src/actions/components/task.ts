// @description execute a specific task

import { TASK_EXECUTED } from "../../utils/constants";

/**
 * Execute a specific action related to the developing package
 */

notify(TASK_EXECUTED, "success", 3000);